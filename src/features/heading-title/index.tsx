import { Flex } from "@/components/Flex"
import { Title } from "@/components/Title"

export const HeadingTitle = () => {
    return (<Flex >
        <Title level={2}>
            smart
        </Title>
    </Flex>)
}